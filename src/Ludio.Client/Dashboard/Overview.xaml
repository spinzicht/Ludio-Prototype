<UserControl x:Class="Ludio.Dashboard.Overview"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Ludio.Dashboard"
             xmlns:cards="clr-namespace:Ludio.Dashboard.Cards"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="30"/>
            <RowDefinition Height="200"/>
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0">Tutorials</TextBlock>
        <ListBox Grid.Row="1">
            <ListBox.ItemTemplate>
                <DataTemplate DataType="{x:Type cards:TutorialCardVM}">
                    <cards:TutorialCard DataContext="{Binding }"/>
                </DataTemplate>
            </ListBox.ItemTemplate>
            <ListBoxItem />
        </ListBox>
    </Grid>
</UserControl>
